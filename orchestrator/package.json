{
  "name": "quorum-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Quorum: calibrated multi-agent treasury council for Casper — off-chain deliberation, on-chain audit trail",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p . --noEmit",
    "review": "tsx src/cli.ts review",
    "demo": "tsx src/cli.ts demo",
    "deploy": "tsx src/chain/deploy.ts",
    "dashboard": "tsx src/dashboard-server.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "casper-js-sdk": "^5.0.12"
  }
}
